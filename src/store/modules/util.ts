import {stateUtil} from '../../types/store'

export const uw = {
  options() {
    //todo
  }
}

export default function (): stateUtil {
  return {
    options: {
      placement: 'bottom'
    }
  }
}
